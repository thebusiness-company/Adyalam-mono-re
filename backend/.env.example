## Edit this file and save as `.env` (do NOT commit secrets).
## Set the correct username/password and database name for your local Postgres.
## Example for a local DB named `adyalam`:
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/adyalam

## If you prefer SQLite for quick local testing, you can use:
## DATABASE_URL=sqlite:///./test.db
